package com.anshtya.jetx.fakedata

//val fakeChats = fakeChatMessages
//    .groupBy { it.sender }
//    .filter { (sender, _) -> sender != "me" }
//    .map { (_, chats) -> chats.maxByOrNull { it.timestamp } }
//    .filterNotNull()
//    .map { chat ->
//        Chat(
//            id = chat.id,
//            name = chat.sender,
//            profilePicture = fakeUsers.find { it.username == chat.sender }?.photo,
//            message = chat.text,
//            timeStamp = DateTimeFormatter.ofPattern("h:mm a").format(chat.timestamp)
//        )
//    }


